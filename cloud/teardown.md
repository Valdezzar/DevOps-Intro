# Lab 10 Teardown

## Hugging Face Space

Open the Space settings in Hugging Face, then choose **Delete this Space** when the lab is graded or no longer needed.

## Cloudflare Quick Tunnel

Stop the `cloudflared tunnel --url http://localhost:8080` process with `Ctrl+C`. Quick tunnel URLs are ephemeral, so no DNS or account cleanup is required.
